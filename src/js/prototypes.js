const fn = function() {};

console.dir(fn);

const arrow = () => {};

console.dir(arrow);

class fnclass {}

console.dir(fnclass);

const btn = document.createElement('button');
btn.textContent = 'qweqweq';
document.body.appendChild(btn);

console.dir(btn);
